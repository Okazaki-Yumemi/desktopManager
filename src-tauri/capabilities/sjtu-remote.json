{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "sjtu-remote",
  "description": "Receive-only bridge for the SJTU calendar sync webview (M12): the university page may push its own calendar JSON via sjtu_receive. No reads, no plugins, no other commands.",
  "windows": ["sjtu"],
  "remote": {
    "urls": ["https://my.sjtu.edu.cn"]
  },
  "permissions": ["allow-sjtu-receive"]
}
