# DesktopManager v1.0.0

轻量级 Windows 桌面工作台管理器：桌面整理（元数据）+ 专注 + 任务 + 日历 + 上海交大课程表同步。不移动你的任何文件，不上传任何数据。

## 亮点

- **上海交大课程表同步（新）**：在弹出的窗口里登录 jAccount，课程表与校历自动同步到日历页的「交大日程」侧边栏；实时显示「正在上课 / 下一节课」倒计时，上课前 10 分钟提醒（提示音 + 通知）。登录凭据只保存在系统 WebView 中，本应用不读取账号密码，数据不出本机。
- **桌面整理**：桌面索引与搜索、虚拟集合（拖拽归类）、场景切换、图标布局快照与恢复（自动排列检测保护，拒绝在错误状态下写入）、图标大小三档。所有操作只记元数据，绝不移动、改名任何文件。
- **任务与日历**：任务速记（Ctrl+N）、状态流转、优先级、截止时间；日历周/月双视图、点格新建、任务关联、RFC 5545 ICS 导出。
- **专注**：番茄钟 25/5、50/10、自定义时长与正计时；重启恢复计时、中断与备注、按日统计、完成提示音。
- **外观**：深浅色 + 5 种强调色 + 自定义取色器，密度 / 毛玻璃强度 / 动效 / 图标大小全部可调，全部本地持久化。
- **可靠性**：数据库迁移前自动备份；损坏数据库自动隔离重建（旧文件重命名保留、永不删除）；500 项桌面规模下核心路径全部 ≤ 10 ms（实测）。

## 安装

| 文件 | 说明 |
| --- | --- |
| `DesktopManager_1.0.0_x64_en-US.msi` | MSI 安装包 |
| `DesktopManager_1.0.0_x64-setup.exe` | NSIS 安装包 |
| `DesktopManager_1.0.0_x64.zip` | 以上两个安装包的压缩包 |

系统要求：Windows 10 / 11 x64。不需要管理员权限。

## 使用交大课程表同步

1. 打开「日历」页右侧的「交大日程」侧边栏（或「设置 → 上海交大日程」），点击同步。
2. 应用会弹出一个内嵌窗口打开 my.sjtu.edu.cn 的日历页面，在里面登录你的 jAccount。
3. 登录成功后页面会自动把日程推送回应用，窗口自动关闭，侧边栏显示「下一节课」倒计时。
4. 登录凭据保存在系统 WebView 里，下次同步通常免登录。

## 已知边界（诚实声明）

- 交大同步的「登录 → 同步」链路尚未经真实 jAccount 账号验证（发布前无法自动化）；如同步未生效，请提 issue 并附上日志（设置页可见日志目录）。
- 交大日程按接口返回的时间导入；循环课仅导入返回的场次。
- 界面语言为中文；所有数据保存在本地 SQLite，应用不联网上传任何内容。
